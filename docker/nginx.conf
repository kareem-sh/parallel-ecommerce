events {
    worker_connections 1024;
}

http {
    # Requirement 5: before = single app (legacy monolith), after = load balanced pool.
    upstream before_backend {
        server app:9000 max_fails=3 fail_timeout=30s;
        keepalive 16;
    }

    upstream after_backend {
        least_conn;
        server app:9000 max_fails=3 fail_timeout=30s;
        server app2:9000 max_fails=3 fail_timeout=30s;
        keepalive 32;
    }

    map $request_uri $php_upstream {
        ~^/api/before/   before_backend;
        default          after_backend;
    }

    server {
        listen 80;
        server_name localhost;
        root /var/www/html/public;
        index index.php;

        location / {
            try_files $uri $uri/ /index.php?$query_string;
        }

        location ~ \.php$ {
            fastcgi_pass $php_upstream;
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            include fastcgi_params;
            fastcgi_keep_conn on;
            fastcgi_buffering off;
        }

        location /health {
            access_log off;
            return 200 "healthy\n";
            add_header Content-Type text/plain;
        }
    }
}
